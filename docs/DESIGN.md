# 百战天元 · 设计文档

> 本文档回答"为什么这样设计"，不是"代码里有什么"。
> 数值以 `src/core/config.ts` 与 `src/data/` 为准，文档只记录取值理由。

---

## 一、设计目标

一款 8 人同场竞技的自走棋。四条不可妥协的目标：

| 目标 | 具体含义 | 怎么验收 |
|---|---|---|
| **策略深度** | 每回合都有决策张力，不是按固定套路铺完就等着看结果 | 至少 3 套强度接近、风格迥异的流派；羁绊提供机制质变；站位是真实博弈 |
| **节奏与情绪** | 单局 25~40 分钟，有高光时刻，连败有翻盘路径 | 无头模拟验证局长；三星/神装/翻盘有专属视听反馈 |
| **系统完整性** | 经济、商店、升星、羁绊、AI、排名真实咬合 | 无头跑完整局不崩溃；AI 五个性格打出可测量的差异 |
| **新手友好** | 不设引导教程，界面自解释，关键信息一键可达，误操作可挽回 | 全局撤销栈；一键布阵；悬停即详情 |

---

## 二、核心循环

```
准备阶段（无时限）                战斗阶段（~20s）           结算（4.5s）
├─ 配对即时生成（备战期可侦查）    ├─ 玩家的战斗上主战场演出    ├─ 败方掉血
├─ 发钱（基础+利息+连胜连败）      ├─ 其余各场无头跑完，        ├─ 淘汰判定与名次
├─ 每 +4 经验 / 刷新商店           │  战报呈现                 └─ 进入下一回合
└─ 玩家操作：买 / 卖 / 站位 / 刷新 / 升级
```

**关键取舍：玩家只看自己的战斗。**
其余 7 人的战斗在后台无头跑完，结果以"上回合战报"的形式滚动播出。做 4 个分屏战场看似热闹，实际结果是玩家一场都看不清——自走棋的战斗信息密度本来就高，分屏等于全部放弃。

**备战无倒计时，开战完全由玩家手动**（"开战"按钮 / 空格）。思考时间无限的排考
虑优先于节奏强制：想快随时可以走，不想快不被拖——倒计时删去后两者兼得。

**配对在回合开始即生成**：备战期全程可用 nav「阵容」侦查本轮对手（敌情面板同步显示），
不是开战瞬间的黑箱抽签。

**未点选的奇遇恩赐在开战时代选**：与 AI 同一选型纯函数（「老谋」偏好序，item 优先）、
同一发放入口，等价于替玩家按下按钮——不惩罚犹豫，也不让恩赐无声蒸发。

### 回合节奏

两类特殊回合固定编入 30 回合左右的长局，真源为 `Match` 的调度表：

| 回合 | 类型 | 内容 |
|---|---|---|
| 1 | 墨兽轮（引导） | 1 只 1★ 引导兽，攻击力 ×0.08；教学轮败北不掉血，必掉 1 件组件 |
| 4 / 10 / 16 | 奇遇轮 | 全员共享 2~3 选 1 恩赐（见下） |
| 7 / 13 / 19 / 25 | 墨兽轮 | 全体玩家各自单挑同一只墨兽，强度随回合爬升 |

第 1 回合是引导轮：开局人人只有一两个棋子也打得过，装备来源从第一轮就建立，且不存在任何淘汰威胁。第 25 轮是最后一只墨兽，其后是纯 PvP 终局。

### 奇遇轮（PvE 恩赐）

奇遇轮在准备阶段给出 2~3 选 1 的恩赐（同回合全员共享同一份选项，AI 按原型偏好即选，人类非阻塞点选；开战未选由系统代选一个，选型与发放同入口）。六类恩赐全部走既有系统入账、严格守恒，档位按前期 / 中期 / 后期（奇遇轮 4 / 10 / 16 各落一档）：

| 恩赐 | 前期 | 中期 | 后期 | 说明 |
|---|---|---|---|---|
| 金币 | +10 | +16 | +24 | 计入持有金币，参与下回合利息 |
| 经验 | +8 | +14 | +20 | 按升级表结算，可一次连升多级 |
| 组件 | ×2 | ×3 | ×3 | 随机组件入装备栏，与墨兽掉落同源 |
| 成品装备 | 恒定 1 件 | — | — | 合成池随机，装备栏收纳 |
| 顿悟 | +1 级 | — | — | 按升级表精确升 1 级；满级折经验档等值金币 |
| 援军 | 2★ 1 费 | 2★ 2 费 | 2★ 3 费 | 占卡池 3 张入驻备战席；席满按 2★ 卖价折金 |

### 配对规则

洗牌后贪心配对，**优先回避最近两轮交过手的对手**。连续三轮撞同一个人是玩家最烦的体验之一，而这个问题只需要一个"最近对手"队列就能解决。

人数为奇数时，落单者与**墨影**交战——沿用最近一名被淘汰玩家的阵容。若尚无人淘汰则轮空。墨影存在的意义是让落单者也承担掉血风险，否则"轮空"会变成可剥削的免费回合。

---

## 三、经济系统

### 收入构成

```
每回合收入 = 基础 5 + 利息 + 连胜连败 + 胜利 1
利息 = min(5, floor(金币 / 10))
```

**利息是整个策略张力的支点。** 它让"忍住不买"变成一种有回报的决策，从而把"每回合要不要花钱"变成真问题。没有利息，最优策略永远是立刻把手上的钱花光，游戏坍缩成一条直线。

上限 5（对应 50 金）是刻意设计的天花板：它给了存钱一个明确的终点，让"存够了"之后必然进入消费期，避免整局都在攒钱。

### 连胜连败

```
连胜：0 0 1 1 2 3 4
连败：0 0 0 1 2 3 4
```

连败的奖励给得更晚（第 4 场才开始），但上限与连胜相同。这是**连败翻盘路径**的经济基础：一路输下去不是纯亏，它在积累一笔"翻本金"。

**轮空不给连胜奖励。** 否则轮空会变成可期待的好事，玩家会对配对结果产生错误的期待。实现上轮空回合既不涨连胜计数，也不结算连胜档位金，避免"轮空反而领钱"的错位。

### 等级与经验

```
上场人数 = 等级（最高 9）
每回合自然 +4 经验
4 金 = 4 经验
升级所需：2 / 2 / 6 / 10 / 20 / 36 / 56 / 80 / 100
```

每回合 4 点被动经验比同类产品慷慨。理由很直接：单局目标 25~40 分钟，等级爬得太慢会把游戏拖长，而"买经验"作为后期主要金币去处的地位不受影响——它依然是**加速**手段，只是不再是唯一手段。

---

## 四、商店与卡池

### 出货概率（按等级）

| 等级 | 1费 | 2费 | 3费 | 4费 | 5费 |
|---|---|---|---|---|---|
| 1~2 | 100 | — | — | — | — |
| 3 | 75 | 25 | — | — | — |
| 4 | 55 | 30 | 15 | — | — |
| 5 | 45 | 33 | 20 | 2 | — |
| 6 | 30 | 40 | 25 | 5 | — |
| 7 | 19 | 30 | 35 | 15 | 1 |
| 8 | 15 | 20 | 35 | 25 | 5 |
| 9 | 5 | 10 | 25 | 40 | 20 |

1~8 级为业界通行曲线：早期 1 费主导、6 级 2 费峰值 40%、7 级起 3 费主力 35%、8 级 4 费入场。
9 级是「冲顶」档：4 费 40% / 5 费 20%，由 1/2 费各让 5% 支付 —— 8→9 一档 100 经验是全表
最贵的等级门槛（约 25 次买经验），给得起该价的玩家应当看到终局质量。满池单人 Monte Carlo
口径（`npm run sim:shop`）：4 费 3★ @9 级中位 68 次刷新、5 费 2★ 中位 29 次、三星五费（天命）
中位 126 次 —— 天命保持「极罕见但存在路径」的终局面额；3 费追星线不受影响（7/8 级 3 费 3★
中位 82 次，搜牌型玩法的核心承诺）。500 局配对断面见 §十二与 §十三。

### 卡池（8 人共享）

```
1费 44 张 · 2费 34 张 · 3费 26 张 · 4费 18 张 · 5费 14 张
```

共享卡池是自走棋最核心的经济约束：**别人把某个棋子抓走了，你就抓不到。** 所有"看牌""抢牌""卡别人"的博弈都建立在这上面。

同费用档位内**按剩余库存加权**抽取，而不是等概率。这带来一个良性副作用：剩得多的更容易出现，于是不存在"最后一张死活刷不出来"的挫败感。

玩家被淘汰时，其全部棋子（按星级拆成对应张数）**退回卡池**。这让淘汰在体感上成为全场事件——"某某死了，他的牌回来了"，也给存活者一个真实的中期转折。

---

## 五、羁绊系统

17 条羁绊（10 地域 + 7 职业）。**每一条都必须归为五类质变之一，且全表不重复**：

| 类型 | 羁绊 | 质变内容 |
|---|---|---|
| **[站位]** | 刺客 | 改变开局站位与行动逻辑 |
| **[技能]** | 龙渊 / 方士 | 改变技能行为本身 |
| **[经济]** | 剑宗 / 丹鼎 | 改变资源（法力）节奏 |
| **[生存]** | 幽冥 / 天庭 / 妖族 / 墨门 | 改变死亡规则与伤害去向 |
| **[节奏]** | 机关 / 山海 / 兵家 | 改变时间曲线（成长 / 叠层） |

### 大地域羁绊（深度专精）

墨门与兵家是「押注整个板位」的大地域羁绊：9 / 8 名成员几乎占满全场，终极档的强度
对标三星五费 —— 强到值得围绕它构筑，但各有天敌（墨门怕爆发切后，兵家怕续航消耗）。
主矩阵覆盖 6墨门与 5兵家的同金同人口成型强度（7人口 52–58 金），天花板另以 9墨门/8兵家对 9 预设双向验证：

| 羁绊 | 档位 | 三档效果 | 对 9 预设平均胜率（天花板） |
|---|---|---|---|
| 墨门 [3/6/9] | 「兼爱」 | 友军所受伤害 **30%** 转由墨门均摊；墨门减伤 20% | 49.6%（6墨门） / 92.8%（9墨门，9人口天花板） |
| 兵家 [2/5/8] | 「百战」 | 友军击杀全队永久 **+26% 攻 / +18% 攻速**，兵家击杀时 **+52% 攻 / +36% 攻速**（全队光环 **+22% 攻 / +17% 攻速**；攻高甲目标附加 15% 攻击力伤害） | 56.6%（5兵家） / 71.0%（8兵家，9人口天花板） |

**均摊的实现纪律**：分摊伤害带 `noShare`（防递归）与 `silent`（不进事件流 ——
高频小额分摊若发飘字，每一次普攻都会变成一场弹幕）。

**为什么必须是机制质变而不是数值堆砌**：数值羁绊（"+15% 攻击力"）玩家感知不到，它只改变结果不改变打法。机制羁绊（"施法后下次普攻附带法术伤害"）会改变玩家的操作与站位决策，这才是"构筑"的乐趣所在。

### 三个具体的机制设计

**刺客：延迟跃入 + 落地烟遁**

第一版刺客开局瞬间跃入后排，测出来胜率极低。这看起来像数值问题，实际是机制错误：

> 若开局瞬间跃入，刺客会在己方前排接战前独自面对 7 人集火，必死。
> 于是"刺客克后排"这个克制关系永远无法成立——因为刺客活不到兑现价值的那一刻。

修法是加两个常量：`ASSASSIN_LEAP_DELAY = 1.0`（等双方前排咬住再进场）+ 落地 0.7 秒烟遁无敌（保证至少放出一次技能）。

**这条经验值得记住：当一个单位"看起来太弱"时，先问它有没有机会兑现自己的设计意图，再问数值。**

**护盾总量上限 `SHIELD_CAP_RATIO = 0.45`**

护卫 6 的荆棘反伤一度 100% 全胜。根因不在反伤，而在护盾体系提供了约 50% 的额外有效生命，让阵容变成"无论如何都打不死"。**绝对防御是自走棋最忌讳的局面——它让所有博弈失效。**

处理方式是引入总量上限，而不是削弱反伤。这是个结构性约束：它同时对"周期性给盾"的所有机制生效（护卫 6、天庭、玄武），一次修好一整类问题。

**流血的真实伤害（山海）**

山海的流血按**目标最大生命**取值，并结算为**真实伤害**。这是"消耗流"克制高护甲阵容的唯一正当输出手段：破甲和法穿只是"更高效地打穿抗性"，流血则干脆绕过抗性曲线——护盾上限治的是"打不死"，流血治的是"没东西可打"，两者各管一段。

实现上有一条纪律：**DoT 状态必须记录伤害类型**（burn = 法术伤害，bleed = 真实伤害）。缺了这一层，就会出现"文案写真伤、结算走物理"的脱节——玩家按规则文本做出的站位与出装决策，和面板跳出的数字对不上。规则文本与结算路径必须同源，这是"描述由配置回填、不与实现脱节"原则在战斗层的对应物。

---

## 六、装备系统

### 为什么需要墨兽轮

装备不能从商店买 —— 那会让金币身兼两职，经济系统的清晰度就没了。装备必须有独立的产出渠道。

**墨兽轮（PvE）**：1 / 7 / 13 / 19 / 25 共五次（回合节奏见 §二），全体玩家各自单挑同一只墨兽。

它解决两个问题，第二个比第一个重要：

1. 装备的来源
2. **节奏起伏。** 全是 PvP 的对局是一串等强度的对抗，没有喘息点。墨兽轮是"这一轮只要打过这只东西就能拿装备"——目标明确、压力较小，让 30 回合的长局有了呼吸。首回合（引导轮）把这份节奏教学提前到开局。

**墨兽复用现有棋子立绘，烘焙期整体罩染青黛。** 零美术成本得到一整套 PvE 内容，而且它看起来就该是"墨变成的怪物"——与世界观自洽。

**全体玩家面对同一只。** 墨兽阵容在回合开始时生成一次；各自 roll 会导致"有人打 Boss 有人打小怪"，那不是 PvE，是抽奖。

强度曲线：引导轮 1 只 1★ 且攻击力 ×0.08（配合教学轮零掉血，首战是纯粹的装备与节奏教学）；常规轮早期 2 只 1★（好打，让玩家拿到第一批装备的成就感），后期 8 只含 3★（真的能淘汰掉弱势玩家）。

### 掉落

掉落 = **全员保底（胜负同发）+ 胜场追加**，真源为 `match.ts` 的 `BEAST_DROP_SCHEDULE`：

| 轮次 | 全员保底 | 胜场追加 |
|---|---|---|
| 1（引导） | 组件 ×2 + 8 金 | —（输赢同礼） |
| 7 | 组件 ×3 + 12 金 | 组件 ×1 + 4 金 |
| 13 | 组件 ×4 + 14 金 | 组件 ×2 + 6 金 |
| 19 | 组件 ×5 + 16 金 | 组件 ×2 + 8 金 + **成品 ×1** |
| 25 | 组件 ×5 + 20 金 | 组件 ×3 + 10 金 + **成品 ×1** |

**下限口径**：1 成品 = 2 组件（配方均为两两合成）。五轮**全败**也有 19 组件 = 9.5 件成装当量 + 70 金，双双高于"一局至少 9 件成装量级、总掉落金币至少 60"的验收线——下限由表结构保证，不由胜率期望保证；五轮全胜 27 组件 + 2 成品（15.5 件成装当量）+ 96 金。

**输了也有全额保底**不是仁慈，是防雪崩：装备差距一旦在早期拉开，弱势玩家会连输到再也拿不到装备，对局在中段就提前结束了。

### 装备设计原则

**每件成品都必须带一个机制，而不是"属性翻倍"。** 纯属性装备（"+50 攻击"）不改变玩家的决策，只是把已有玩法数值放大；带机制的装备（"击杀回复 18% 生命"）会改变构筑与站位思路。

8 个组件两两组合（含同件）共 36 种配方，**v1.9 起全配方实装**：任何两件组件拖到一起必定合成，"这两个合出来是什么"的探索由配方多样性承担，而不是靠"合不出来"留白。全表只有两件刻意保留的**白板位**——混元珠（纯法强 +56）与紫霄珠（纯暴击）："把一条属性堆到极致"的最朴素路线，各保留一条不被机制绑架的去处。

| 成品 | 配方 | 机制 |
|---|---|---|
| 断魂刃 | 翠玦×2 | 击杀回复 18% 最大生命 |
| 破甲杖 | 翠玦+金锭 | 无视 35% 护甲 |
| 血饮 | 翠玦+紫晶 | 18% 全能吸血 |
| 贯日枪 | 翠玦+灵珠 | 普攻追加 40% 法强法伤 |
| 寒渊镰 | 翠玦+轻羽 | 普攻减速 20% / 2 秒 |
| 缚龙爪 | 翠玦+青莲 | 损血叠攻（每 10% +4%，至多 32%） |
| 流星弩 | 翠玦+法符 | 击杀后 5 秒 50% 攻速（至多 2 层） |
| 赤练鞭 | 翠玦+赤绳 | 普攻上易伤 12% / 3 秒 |
| 不动明王 | 金锭×2 | 物理减伤 15% + 全类型减伤 8% |
| 青圭杖 | 金锭+灵珠 | 施法得 10% 生命护盾，技能增伤 10% |
| 追风履 | 金锭+轻羽 | 每秒 +1.5% 攻速，至多 +24% |
| 狮心盾 | 金锭+紫晶 | 承伤转蓝 ×1.4 |
| 焦尾琴 | 金锭+法符 | 护甲 +26，初始法力 +18，每秒回蓝 +6 |
| 玄铁重甲 | 金锭+赤绳 | 每 5 秒净化一个减益 |
| 玄武甲 | 金锭+青莲 | 受到物理伤害反弹 20% |
| 混元珠 | 灵珠×2 | 无 —— 纯法强 +56（白板位） |
| 紫电镰 | 灵珠+轻羽 | 施法后 5 秒 45% 攻速（至多 2 层） |
| 惊雷锤 | 灵珠+紫晶 | 技能可暴击（30% / 1.4 倍） |
| 太虚经 | 灵珠+法符 | 技能增伤 18% |
| 引魂灯 | 灵珠+赤绳 | 施法治疗生命最低友军（70% 法强） |
| 血魂珠 | 灵珠+青莲 | 22% 全能吸血 |
| 疾风弓 | 轻羽×2 | 普攻叠 7% 攻速，最多 5 层 |
| 九尾面 | 轻羽+法符 | 攻速 +20%，初始法力 +14，施法后下次普攻必爆 + 300% 法强法伤 |
| 垂天翼 | 轻羽+青莲 | 开战 8 秒 30% 攻速 |
| 拂尘扇 | 轻羽+赤绳 | 每 4 次普攻缴械 1.2 秒 |
| 霜翎环 | 青莲+紫晶 | 普攻命中回复 1.5% 最大生命 |
| 巨灵冠 | 青莲×2 | 每秒回复 1.5% 最大生命 |
| 翠玉带 | 青莲+法符 | 生命 +400，治疗提升 60%，每秒回蓝 +2 |
| 鹤龄镜 | 青莲+赤绳 | 首次阵亡 2 秒后以 15% 生命复活 |
| 回天灯 | 法符×2 | 法强 +25，初始法力 +24，每秒回蓝 +4，治疗提升 90%，治疗溢出全额转护盾 |
| 紫金炉 | 法符+紫晶 | 普攻命中额外回蓝 6 点 |
| 墨龙旗 | 法符+赤绳 | 初始法力 +16，魔抗 +14，生命 +100，开战全体友军 8 秒 22% 减伤 |
| 紫霄珠 | 紫晶×2 | 无 —— 暴击率 30% + 暴伤 50%（白板位） |
| 摄魂铃 | 紫晶+赤绳 | 受普攻 30% 概率眩晕攻击者 1 秒 |
| 玄冥衣 | 赤绳×2 | 全类型减伤 12% |
| 影袭 | 轻羽+紫晶 | 暴伤 +28%，无视 25% 护甲 |

配方的强度纪律（`npm run sim:items` 配对验收，2026-09-01 全表复测）：**每件成品的边际胜率必须高于"两组件拆开分装两人"之和**——当前 31/36 严格正收益，其余 4 件（九尾面 -0.7%、翠玉带 -0.5%、混元珠 -0.3%、紫电镰 -0.1%）处于 ±1 个百分点的噪声带边缘；全表极差 9.9%（阈值 16% 以内）；组件自身全部正收益。法符是唯一带机制包的组件（初始法力 + 回蓝），也是全表最强组件（单件 +9.0%）——它的拆分价值结构性高于其他组件，8 条法符系配方因此按「合成增益 ≥ 0」为锚验收（回天灯定档：单件 +17.6%，对拆分线 ≈+18% 进噪声带）。

### 两条交互规则

**一个棋子最多 3 件。** 这是通行上限，意义在于"装满一件装备"是个明确、可完成的决策目标。没有上限，装备就只是一坨可以无限堆的数值。

**成品可以卸下，且拆回两个组件。** 很多同类产品里成品一旦装上就拿不下来，理由是"防止玩家把装备当流通货币"。但本作的设计目标是**误操作可挽回**——一条不可逆的操作会逼玩家在每次拖拽前犹豫，这比允许拆装的代价大得多。

**主动卸装统一严守器匣容量。** 批量卸载（整体回匣）与单件卸载（点棋子装备位卸一件）同属"玩家主动卸装"，容量口径一致：器匣放不下即**整体拒绝并说明原因**——成品拆回两组件占 2 格，只有腾出足够空间才放行，绝不"卸一半丢一半"。与之相对，卖出 / 合成吃子 / 淘汰剥离等**系统回收路径允许溢出**（装备不该因卖牌蒸发，守恒优先于版面；超出格数的装备不可见但仍入存档）——分界是"玩家主动要求 vs 系统被动回收"。

**器匣内拖拽即合成。** 全配方之后，组件 A 拖到组件 B 上原地合出成品（结果落在靠前的格位，可撤销、与装配合成共用同一场"神兵"演出）——玩家不必先找一个棋子当"合成台"。拖到棋子上仍是装配路径；身上的可合成组件会优先吞掉新拖入的组件（合成优先于占格）。

### 装备给谁：放大，不要补短

AI 分配装备的判据只有一条：**装备应该放大这个棋子本来就在做的事**，而不是补它的短板。给前排塞攻击力、给法师塞护甲，都是典型的"看起来有道理实则浪费"。

算法是把棋子和装备都投影到「普攻 / 法术 / 承伤」三条轴上取点积——同向则相乘变大，错配则互相抵消。再乘以棋子权重的 0.35 次幂（压平量级，否则三星棋子会吃掉所有装备，因为装备给 carry 的收益最大）。

### 装备在内核里的三层生效

与羁绊**共用同一套机制**，不引入新概念：

1. 面板属性 → 并入 `Unit` 静态面板
2. 状态修正 → 累加进 `Unit.trait`（与羁绊同一份 TraitState）
3. 行为钩子 → 注册到队伍 hooks，与羁绊钩子并列

不新开一套"装备状态"，是因为羁绊与装备在内核里本来就是同一类东西。多一套并行结构，只会让减伤/增伤的计算顺序变成需要维护两遍的隐患。

**同名叠加口径（全表统一）**：面板属性与状态修正按件**求和**（两件玄甲就是两份护甲）；
行为钩子同一单位只结算一次、参数取**最强一件**（如两件贯日枪仍只追加一件的 40% 法伤）
—— 比率类参数取 max 是防乘区爆炸的既定裁决。图鉴与合成提示按此口径描述，不承诺"叠加"。

---

## 七、战斗系统

### 确定性内核

```
30Hz 固定步长（TICK_RATE = 30）
唯一随机源：Rng（mulberry32），战斗内核（core/ game/ data/）不存在 Math.random()；渲染与音频属于表现层，其随机不受确定性契约约束
内核只产出带 tick 时间戳的事件流
行动次序：单位按 uid 升序行动，奇偶 tick 轮换先后手 —— 近似同时结算，残差落在先手噪声带内（§十二 51.5% 的实测口径即含此机制）
```

渲染层单向消费事件流，**永远不可能影响战斗结果**。这条契约带来两个直接收益：

1. 平衡模拟跑的就是玩家实际看到的战斗（同一份配置），模拟数据因此可信。
2. 战斗结果可复现，bug 可定位。

### 法力

```
每次攻击命中 +10
承受伤害转化： (伤害 / 最大生命) × 32，单次上限 15
每秒自然回复 2
施法后锁定 0.5 秒（给技能特效留出起手时间）
满蓝到生效延迟 0.35 秒（吟唱）
```

"受击回蓝"这个机制容易被忽略，但它决定了**劣势方不是纯粹的挨打**：被压制的单位反而攒蓝更快，有机会先手翻盘。这是连败翻盘路径在战斗层面的对应物。

### 伤害

```
物理： 实际 = 原始 × 100 / (100 + 护甲 × (1 - 破甲)) × (1 - 物理减伤)
法术： 实际 = 原始 × 100 / (100 + 魔抗) × (1 - 法术减伤)
真伤： 无视抗性与通用减伤（dr 类状态不生效，易伤类增伤仍生效）
抗性上限 220；减伤按类分别以 90% 为上限（物理 / 法术 / 通用各自钳制），多类之间串乘生效
```

**破甲（剑宗 4 档，15%）**是重甲阵容的天然克星。没有破甲机制，堆护甲会变成无解的线性最优解。

### 星级

```
生命 ×1 / ×1.8 / ×3.24
攻击·法强 ×1 / ×1.45 / ×2.1
技能 ×1 / ×1.5 / ×2.25
```

生命倍率刻意高于攻击倍率：**三星是"又肉又能打"，不是一刀秒。** 如果三星能秒人，战斗会退化成"谁先三星谁赢"，博弈空间被压缩到零。

---

## 八、AI 对手：让机器像人

五个性格原型。**目标不是下出最优解，而是像一个有性格的人在打。** 一个每次都打出最优解的对手，玩家感受到的不是挑战，是一堵墙。

| 原型 | 称号 | 打法 | 等级节奏 | 搜牌 | 三星偏好 |
|---|---|---|---|---|---|
| `aggro` | 血勇 | 早早提人口压场面，靠连胜滚雪球 | 快（1.15×） | 少 | 1.0 |
| `econ` | 守财 | 死守 50 金吃满利息，中期突然发力 | 慢（0.95×） | 很少 | 1.0 |
| `balanced` | 老谋 | 跟着发牌走，什么都沾一点 | 标准 | 中 | 1.0 |
| `hyperroll` | 孤注 | 三星猎人，钱几乎全砸在刷新上 | 标准 | 极多 | 3.6 |
| `greedy` | 钓叟 | 疯狂冲人口，靠高费卡翻盘 | 很快（1.25×） | 少 | 1.0 |

三条让 AI 有"人味"的设计：

1. **性格原型** —— 你会感觉"那个守财的又在那攒钱"，而不是"七个一样的机器人在打我"。
2. **局势感知** —— 同一个守财 AI 在 20 血时的行为和 90 血时完全不同。血少了会掀掉存钱底线去搏，人少了会产生名次焦虑。这是"他在挣扎"的观感来源。
3. **决策噪声** —— 每个原型有 noise 系数，会偶尔做出次优选择。**完全理性的对手看起来最假。**

### 让 AI 真的会凑三星：三个连环坑

「孤注」最初被定义为"停在低人口把低费牌搜穿"（等级上限 6）。模拟结果：40 局 0 冠，平均名次 5.75，明显在送。原因是这个游戏里**上场人数 = 等级**，6 人口对 9 人口是纯粹的人口碾压。

修正方向是重新定义这个性格——它是"愿意为凑三星多付钱的三星猎人"（`mergeBias`），而不是"自废人口"。但改完仍然凑不出三星，往下挖才发现三个连环问题：

1. **备战席被杂牌塞满后永久卡死。** AI 开局几回合买进 9 张互不相关的牌，之后再也腾不出连续三格去凑任何一个三星。症状是"它一直在刷新但从来凑不出东西"。修法：每回合开始前清理备战席，始终留 3 格（`tidyBench`）。

2. **清理逻辑把合成进度当垃圾卖掉了。** 它评判"这张牌值不值得留"时只看牌本身有多强。于是"场上已有一个 2★ + 备战席一张 1★"里的那张 1★ 因为最弱而被优先卖掉——而它恰恰是通往**第二个** 2★ 的唯一进度。修法：把"已有更高星级同名"显式计入留下价值。

3. **合成估值被判序掩盖。** `mergeValue` 先判 `one >= 2` 再判 `two`，导致同样上面那种局面只拿到 8 分（一张孤立一星的价），而不是"通往第二个二星"的价。修法：判序从高星级往下走。

三处修完，「孤注」的终局三星数从 **0.02 个/局 涨到 0.19 个/局**，是其他原型的 2~3 倍——"三星猎人"这个人设才真正成立。

> 教训：三个 bug 单独看都只是"数值不太对"，合起来把一个核心情绪点（三星高光时刻）整个抹掉了。
> 这类问题只能靠"把人设变成可测量的指标"来发现——所以在模拟里加了三星数统计，而不是只看名次。

### 卡池容量决定三星是否可能

32 个棋子分摊 8 个人的抽取，稀释度远高于同类产品（通常一个赛季 13 个棋子左右）。按 29/22/18/12/10 实测，全局面三星期望只有 0.07 个/人。上调到 **40/30/22/15/12** 后，刻意追三星的玩家能稳定凑出，随手玩的玩家偶尔能撞到一个。扩军到 64 棋子后商店稀释翻倍，卡池同步上调至 **44/34/26/18/14** ——「想追的牌见得到」这条体感不随棋子数回落。

**三星是设计里最重要的高光时刻之一，它不能是"理论上存在但实际见不到"。**

---

## 九、站位与博弈

- **前排 4 行的纵深**由职业自动推导（护卫 0 → 丹师 0.88），玩家不必手摆每一格，但可以逐格微调。
- **刺客跳后排**，所以"把核心输出放在角落"是有效但可被针对的策略。
- **同名棋子允许同时上场**。羁绊按「是否拥有该棋子」唯一计数，同名堆场不重复叠羁绊；
  多余的同名是候补合成料，三合成跨场上 / 备战席任意组合。自动布阵与买入自动上场
  同步放开（1.9.1）：布阵先保证 distinct 羁绊覆盖（每个名字最强一张），人口仍有
  空位时按战力补同名重复张 —— 堆场是玩家的战术决定，整理不得撤下它；
  溢出（人口+席位都满）才卖。

**一键布阵**的存在理由是新手保护：没有前排，后排会在第一轮技能循环内被刺客和 AOE 清干净。这是新手最常见的"我明明棋子很好却打不过"的原因，而它完全可以通过一个按钮避免。

---

## 十、新手友好

| 措施 | 说明 |
|---|---|
| **全局撤销栈** | 每一次买入 / 卖出 / 移动 / 刷新 / 升级前快照整个玩家状态（棋盘 32 格 + 备战席 9 + 等级/经验 + 金币 + 商店 + 器匣 + 共享卡池计数 + 本回合未领取的奇遇恩赐 + **随机流游标**），`Ctrl+Z` 或按钮回退，最多 30 步；卡池随快照对称回滚，买入/卖出撤销不再造成卡池漂移 |
| **一键布阵** | 按职业纵深自动排列，并强制保留至少 2 个前排 |
| **悬停即详情** | 棋子属性、羁绊、技能描述（数值由配置模板回填，不会与实现脱节）；已穿装备以三格 24px 图标槽呈现（`itemIcons` 同一烘焙管线，空槽淡框），格下常驻短名、悬停看完整效果与合成路径 |
| **点击见羁绊成员** | 对局左轨羁绊徽章悬停出当前档效果笺，**点击**钉住成员卡：全名单立绘网格（该羁绊每名棋子），拥有者金框、未拥有压暗，头部给「已上阵 x / 全 N」；ESC/点空白/再点同徽章收起，买子卖子原位刷新。成员卡钉住期间棋盘快捷键（D/F/E/空格/Z/1-5）同样让路——浮层打开的统一让路口径覆盖一切信息浮层。图鉴「羁绊」册每行点击展开同一成员网格，悬停任一成员看其详情 |
| **出售双通道** | 拖到右下朱印，或点选棋子后在钉住详情卡按「出售 · N 金」——2★/3★ 第一次点变确认态，金额自含绝对数值 |
| **不可逆动作确认** | 主菜单「新对局」在有存档时变「确认覆盖存档？」；对局内「放弃对局」变「确认放弃？」——确认态只作用于下一次点按 |
| **落点预演** | 拖拽时目标格实时染金边（允许）或红边（拒绝并说明原因） |
| **买入自动上场** | 人口有空位时自动上场，可在设置里关掉 |
| **操作提示** | 商肆注脚常驻键位参考：`刷新 N 金 · 直购 1-5 · 撤销 Ctrl+Z`（`D`/`F`/`E`/`空格` 在按钮文案上自明，`ESC` 逐层关窗）；设置页脚为完整出处 |
| **双档隔离** | 普通档与每日挑战档分键存取：任一模式终局/放弃只清本模式进度，玩每日挑战不再覆盖普通档（v3.1） |

关于"误操作可挽回"的取舍：撤销栈选择**快照整个玩家状态**（棋盘 32 格 + 备战席 9 + 等级/经验 + 金币 + 商店 + 器匣 + 共享卡池计数 + 本回合未领取的奇遇恩赐 + 随机流游标），而不是记录反向操作。反向操作需要为每个动作写一份撤销逻辑，漏一个就是 bug；快照则是"一处正确，处处正确"。随机流游标（Rng.state）随快照一起定格与回写——准备阶段随机流的唯一消费者是玩家动作本身，游标不回滚的话，"刷新-撤销"循环会落在前进过的随机流上，等于零成本预览后续商店；游标随快照对称回滚后，撤销再重摇与被撤销那次逐位一致，探店通道被结构性封死。奇遇恩赐的领取本身不单独入栈，但恩赐 offer 本体随快照出入栈——领取后撤销会把 offer 还原回快照时刻的对象，重选与重摇都得到同一份结果，不存在重复发放、也不存在"撤销把已领恩赐吞掉"的蒸发路径。

---

## 十一、平衡方法论

数值不靠感觉。平衡走「**迭代式调参 + 统计反馈**」：每次改动 → 无头批量模拟 →
读统计量 → 决定下一步，逐步逼近目标区间。模拟器与实机跑的是同一份内核
（无头契约的直接红利），所以模拟数据就是玩家实际体验的数据。

**目标是相对平衡与百花齐放，不是绝对平均**：① 阵容带 [44%, 58%] —— 每套构筑
胜率落带（下限=不是陷阱选择，上限=不是自动赢），这是硬门；② 边带 [20%, 80%] ——
硬克制的健康形态是 70/30，规范帧下的残余硬边按「结构性克制」口径管理（见 §十二：
数值与装备均不可达，反制位在玩家能动性）；③ 极差只是诊断量 —— 岩纸布
（互有克制的纹理）比「人人 52%」健康，压平一切会毁掉身份。工具链的
`matrix`/`sweep` 报告直接输出这两条带的越带读数（balance/README.md D4）。

### 工具链

工具链 2026-09-02 重建为 `npm run balance -- <command>`（`balance/` 目录，进程池
并行 + SQLite 工件库；旧 `sim:*` 命令名保留为别名，决策记录见 balance/README.md）：

| 命令 | 统计量 | 用途 |
|---|---|---|
| `matrix` | 对拍矩阵（9套）、综合胜率、阵容带/边带越带读数、超时率 | 阵容维基线，每对 200 局起步，入库 |
| `sweep` | OAT/组合各旋钮的 Δ 胜率（噪声带标注）与边际斜率 | 找敏感参数、迭代候选组合，入库 |
| `ab` | 指定配对集合的 CRN 配对移动量（百分点） | 克制边的定向压缩验证 |
| `units` | 逐单位场均伤害/承伤/类型构成/施法/生存率 | 棋子维：随 matrix 入库的单位榜 |
| `traits` | 逐羁绊 scale=0 压制 vs 基线的边际胜率贡献 | 羁绊维：每条羁绊值多少 |
| `items` | 单件边际胜率、合成增益、同件堆叠曲线、异件 logit 边际 | 装备维三层检验，入库 |
| `trend` | 跨版本断面移动（按阵容名对齐） | 读 SQLite 工件库 |
| `selftest` | 数据自检/确定性/CRN/进程池与串行逐位一致/先手公平 | 战役前门禁（五关） |
| `match` / `shop` / `gold` / `diag` / `probe` / `bench` / `audit` | 局长与 AI 极差 / 商店概率 / 造价带 / 单场诊断 / 吞吐 / 羁绊可达性 | 对局层验收与速查 |

### 四条度量纪律

这些是数据可信的前提，缺一条整个工具链都在测噪声：

1. **胜率必须双向取平均。** 上方与下方的起始行不同，存在结构性的站位偏差；
   `matrix[i][j]` 记录的是"j 在下方时的胜率"，只读单方向会把强弱读反。
2. **对拍必须落在同一造价带内。** 六套预设统一在 52~58 金（`gold-value.ts` 核算）。
   造价差 20 金的两套阵容对拍，测出来的是钱不是羁绊。
3. **配对设计 + 共随机数（CRN）。** 装备的效应量只有几个百分点，独立抽样的方差
   足以把它整个盖住。同一批种子下"装 vs 不装"逐种子相减，方差被配对消掉 ——
   这也是扫描器能用较小 N 分辨 ≥2% 移动的原因。
4. **边际收益用对数赔率（logit）度量。** 胜率是逻辑斯蒂量：同样 +5% 胜率，
   在 50% 基线上远比在 85% 基线上值钱。跨基线比较胜率差会系统性误判"递减/爆炸"。
   同件堆叠（机制不叠加，应递减）与异件协同（乘区叠加，允许协同但须有界）
   是两个独立检验，不能混在一个数字里。

### 调参顺序

1. 内核正确性（公式口径、超时裁定、行动次序）—— 一切统计的地基；
2. 造价带对齐；
3. 阵容带 [44,58] 硬门 + 边带越带读数（当前阵容带越带 0 套；残余硬边按结构性克制口径管理，见 §十二）；
4. 装备专项（36 配方合成正收益、全表极差、递减曲线）；
5. 极端克制边的定向 A/B（`sim:ab`）；
6. 平台期确认：OAT 扫描若全部落在噪声带内，说明已到局部最优 ——
   此时继续拧数值只会伤及中段对局，要改善只能动机制。

### 工程注记：Phaser 的 Graphics 没有缓存

WebGL 下 `Graphics` 每帧重放整个命令缓冲，一个 `fillRoundedRect` ≈ 45 条命令。
纪律：**形态不随帧变化的图形一律烤成纹理**（`render/view/bake.ts`），
只有数值条填充、拖拽高亮等少数元素保留实时 Graphics。
该纪律把战斗场景从 67 FPS 上限（9,848 条指令独占 8.6ms/帧）带到 181 FPS。

---

## 十二、当前平衡数据

九套预设覆盖全部 17 条羁绊（10 地域+7 职业），造价 52–58 金同带，CRN 双向对拍。
工作流与全部历史断面存于平衡工具链工件库（`balance/out/balance.db`，`trend` 命令跨版本读数）。

### 战斗节奏机制

- **超时上限 50 秒**：加时窗（30 秒起）伤害每秒线性放大 50%，治疗/护盾/回复产出衰减 50% ——
  加时是决胜时刻，「拖」在加时窗里逐步失去屏障。实测全部对拍超时率 0.00%（原 2.8%：
  幽冥对护卫 77%→0%，护卫对墨门 63%→0%），原「超时裁定局」全部在 41~50 秒内打出真实胜负。
- **四个条件化通道**（绝对数值见各羁绊图鉴）：
  刺客「攻其不备」跃入突袭（不受伤害分摊）与「猎盾」（对持盾目标附加其最大生命的物理伤害）；
  幽冥「余烬」（复活后 4 秒受伤增加）；机关「构装护体」（构装体魔抗）；兵家「攻城」（对高护甲目标附加攻击力伤害）。

### 流派强度（9 套，每对 200 局，CRN 双向，种子 20260902）

| 流派 | 综合胜率 | 上方 / 下方 |
|---|---|---|
| 兵家 · 5兵家 | 56.6% | 56% / 57% |
| 机关 · 4机关3神射 | 56.0% | 51% / 61% |
| 护卫 · 6护卫2山海 | 54.7% | 57% / 53% |
| 墨门 · 6墨门 | 49.6% | 49% / 51% |
| 快攻 · 4剑宗3刺客 | 48.1% | 47% / 49% |
| 法爆 · 4龙渊4方士 | 48.0% | 52% / 44% |
| 妖族 · 4妖族4武将 | 46.4% | 46% / 47% |
| 幽冥 · 4幽冥3术士 | 45.4% | 47% / 44% |
| 天庭 · 4天庭 | 45.2% | 52% / 38% |

**阵容带 [44%, 58%] 九套全部带内，极差 11.4%，超时率 0.00%，先手 50.7%（随机镜像
600 局，噪声带 ±4.1p）。** 数值定档全部经 `balance sweep` CRN 配对迭代 + 换种子
n=200 复测（非种子过拟合）。

### 克制关系与结构性克制

克制由机制形成：刺客跳后排克法爆，护卫反伤克攻速，真伤克高魔抗，围攻克高护甲。
九套矩阵整体呈网状而非单一环；每套构筑都有明确的猎物与天敌（身份表见 balance/README.md D4）。

**结构性克制（边带校准口径）**：规范帧（同造价、无装备、自动站位、确定性种子）下
存在约 4~7 条 100% 硬边（幽冥→法爆、兵家→法爆、护卫→兵家、机关→墨门等，随种子
在 95~100% 间浮动），其余越带边落在 84~99%。这批边对 OAT 数值旋钮（护甲/破甲/
续航/血量/暴击全档）与装备对装试验均零响应 —— 是确定性同构对局的结构判定，不是
数值差。通道层（刺客双通道/余烬/构装护体/攻城）已吃掉其中三条（快攻侧两条 + 法爆
对机关），并把极差从 28.0% 压到 11.4%。残余硬边的反制位在玩家能动性：侦查换站位、
针对配装、经济压制 —— 规范帧刻意剥离了这些，是最严苛压力测试而非实战体验读数。
后续若进一步收窄，走 thornResist 式条件化通道逐边立项（证据与候选在工件库）。

### 装备（配对设计，CRN，44 件全表）

- 单件边际：成品 +4.0% ~ +17.1%（均值 8.5%），组件均值 +2.3%（法符 +7.2% 最强组件）；
- **合成正收益 36/36 全表**（法符第三轮定档：法强 34、生命 60、初始法力 8 之后，
  双法符拆分线回落到全部 36 条配方之下）；成品极差 10.4%（≤16% 门），合成倍率 3.69×；
- 已知曲线性质两条（本代引擎结构，非越界）：断魂刃「击杀-回复」引擎同件堆叠第 2 件
  边际反超首件（更多攻击→更多击杀→更多回复的正反馈，第 3 件恢复递减）；断魂刃+破甲杖+
  血饮的 穿甲/吸血 乘区协同 logit 边际为首件 1.76 倍。硬门（合成正收益/极差/倍率）全绿。

### 整局（8人，500 局）

平均 32 回合，约 27 分钟；冠军等级 9.0；冠军场上 9 人；AI 五原型平均名次 4.05–4.82，极差 0.77，性格分化达标。

### 验收快照

| 项 | 结果 |
|---|---|
| TypeScript strict 类型检查 | ✓ |
| 架构边界 check:boundaries | ✓ 77 个 src 文件（七层 + 根组合层，覆盖计数断言） |
| 核心行为测试 vitest | ✓ 142 项（战斗/经济/资源/存读档/回放/交互/平衡 + 平衡工具链框架与终态钉值） |
| 生产构建 | ✓ app gzip 113KB + phaser 分包（gzip 340KB） |
| 战斗 22 单位 @1080p | ✓ 6.6ms/帧，0% 超 16.7ms |
| 准备满编 | ✓ 6.4ms/帧 |
| 40 次重启内存 | ✓ 无泄漏 |
| 控制台 | ✓ 零报错零警告 |
| 音频 | ✓ 16 音效 + 3 BGM，三总线 |

---

## 十三、已知边界

9 套预设同金同人口对拍，极差 26.9%（500局/对，CRN 双向，先手 51.5%；2026-09-01 复测）。单局 31 回合、26 分钟，冠军等级 9.0，AI 五原型名次 4.1–4.8 分化达标（名次极差随样本在 0.6~1.0 波动，均为性格真实分化）。机制上，围攻·破阵（`gangAtk=6`）与木石之躯（`thornResist=0.65`）使机关对护卫呈均势；其余克制由羁绊机制形成，数值处于平台期。

## 十四、天命单位（五费三星）

三星五费是终局面额：除常规三星倍率（生命 ×3.24 / 攻击法强 ×2.1 / 技能 ×2.25）外，
再叠加「天命护持」包（`config.LEGEND_T3`，单一真源）：

| 项 | 值 | 说明 |
|---|---|---|
| 生命 / 攻击 / 法强 | ×2.0 / ×2.0 / ×2.0 | 叠加在星级倍率之上（3★ 合成生命 ×6.48、战力 ×4.2） |
| 技能数值 | ×1.25 | skills.skillRaw 出口统一结算；墨兽/召唤物不享受 |
| 开战护盾 | 35% 最大生命 | 开局即有，吸收型；无到期时点，只能被伤害打掉 |
| 全能吸血 | +20% | 普攻与技能同吃 |
| 免疫控制 | 是 | 眩晕/沉默/缴械/缓速/嘲讽全部拒绝 |
| 体量 | ×1.2 | 渲染层与数值同源；头顶 UI（血条/星标/装备行/冠弧）为恒定世界尺寸，不随体量放大 |

[v1.9 大加强定档]（`npm run sim:legend`，9 预设 × 96 种子 × 队号与 uid 同翻镜像）：旧包
（×1.6 / ×1.5 / 盾 25% / 吸血 15%）下「同阵容五费 2★→3★」的镜像胜率读数偏弱、
追三的改变感不足；新包把天命组镜像推到 98%（对照组——四费/三费三星——82.7%），
且在未饱和的配对上可分解出天命包在纯星级缩放之上的净贡献（法爆 +20.3p、妖族 +8.9p）。
镜像纪律：完全对称的镜像会触发"低 uid 必胜"的确定性结算伪影（同构阵容互相湮灭时
胜负由 uid 平局裁决决定），第二局必须同时翻转队号与 uid 高低，否则测量整体虚高几十个百分点。

设计口径：三星五费应当"大概率接管终局"——数值足以碾压常规三星，
免疫控制保证关键大招不被打断；但护盾与吸血是**强度**而非**无敌**，
集火与真伤仍是解法。常规定位数值经 `sim` 矩阵回归（预设不含三星五费，矩阵不受影响）。

**墨兽排除**：天命判定要求非 monster 输入——墨兽轮后期池含五费且 16 回合起有
12%~30% 概率出 3★，不排除则 PvE 静默获得天命包，难度尖峰绕过平衡前提。
数值层（`unit.ts`）与渲染层（`unitLayout.isLegendUnit`）同口径排除，
契约见 `legend-qol.test.ts`「3★ 五费墨兽不获得天命」。

### 三星四费「登峰」（T3_ELITE_COST4）

四费 3★ 在常规三星倍率之上再乘 **×1.15 生命 / ×1.15 攻击法强**（`config.T3_ELITE_COST4`
单一真源；AI 估值 `powerScore` 同源）。动机：天命落地后四费追三的决策价值塌了——
同为终局投入，四费 3★ 打不过任何成型天命。登峰**只给数值乘区，不给机制包**
（无技能乘区/免控/盾/吸血），与天命保持「质」的距离：它是便宜一档的可靠终局，
不是第二套天命。定档依据：sim 矩阵（2★ 预设口径）极差移动为 0（作用面在实战追三），
数值契约见 `robustness.test.ts`「三星四费登峰」。墨兽排除与天命同口径。
