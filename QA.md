# 百战天元 · QA 质量手册

本文档定义本项目的质量标准与验证方式：**不变量 → 测试 → 模拟 → 度量** 四层防线，
每一条都给出复现命令。所有数字均由可复现的脚本或浏览器内探针采集，不是估计值；
质量不是一次性的验收动作，而是任何人改完代码后都能在两分钟内重跑的固定流程。

## 0. 验证环境

| 项 | 值 |
|---|---|
| 运行时 | Node ≥ 20，Phaser 3.80，Vite 5，Vitest 2 |
| 类型检查 | `tsc --noEmit`，strict 全开，零错误 |
| 设计分辨率 | 1920 × 1080（Phaser `Scale.FIT` + `CENTER_BOTH`） |
| 战斗内核 | 30 Hz 固定步长，mulberry32 单一随机源 |
| 平台 | 桌面 Chrome / Edge（WebGL 优先） |

## 1. 快速验证回路（改完代码必跑）

```
npm run typecheck   # 类型层
npm test            # 158 项回归（~10s）
npm run build       # 生产构建（含类型检查）
```

平衡相关改动另跑：`npm run sim`（流派矩阵）、`npm run sim:items`（装备）、
按需 `sim:sweep` / `sim:ab`（方法论见 DESIGN §十一）。

## 2. 不变量（测试盯着的契约）

### 2.1 确定性

- 同 seed + 同输入 ⇒ **事件流逐字节一致**（JSON 逐条比对）；
- 不同 seed ⇒ 结果必须分歧（证明随机源真的在起作用）；
- 内核（`core/` `game/` `data/`）不存在 `Math.random()`，唯一随机源是注入的 mulberry32。

### 2.2 资源守恒

任何操作路径下，棋子与装备不凭空产生或消失：

- 共享卡池：买入 −1、卖出 +1、三星合成 −3（消耗三张）；
- 装备：合成进器匣、卖出含装备的棋子返还全部组件、墨兽轮掉落守恒；
- 自动整理 / 买卖回滚 / 整局模拟路径全部有专项断言。

### 2.3 战斗公式口径

这是数值正确性的核心，每个口径一个测试：

- **暴击单结算**：普攻掷骰一次、`forceCrit` 传入结果，dealDamage 只乘一次倍率 ——
  否则暴伤变倍率平方、暴击率变 1−(1−p)²；
- **物理减伤**：`100/(100+护甲×(1−破甲))`，护甲/魔抗上限 220；
- **真伤口径**：无视抗性与通用减伤，易伤类增伤仍生效；DoT 的伤害类型随状态走
  （burn=法术、bleed 可为真伤），不按状态种类硬编码；
- **吸血语义**：普攻吃普攻吸血 + 全能吸血，技能/DoT/反伤只吃全能吸血；
- **超时裁定**：按剩余生命比例排序裁定，比例差 ≤2% 判平局。

### 2.4 撤销与经济

- 撤销 = 完整玩家状态快照（含卡池计数），任意操作序列回退后逐字段一致；
- 异常路径（撤销栈中的非法操作）不破坏快照一致性；
- 利息 / 连胜连败 / 经验表与 `config.ts` 常量一致。

### 2.5 扫描框架自身

- 补丁写入 → 战斗 → 还原：数值逐字段往返一致，中途抛异常也能还原；
- 非法路径（不存在的字段 / 非数字目标）必须抛错而不是静默；
- CRN：同种子同配置结果逐数字一致；参数覆盖确实改变战斗结果。

## 3. 回归测试清单（vitest，17 文件 158 项）

| 文件 | 覆盖 |
|---|---|
| `battle-formula.test.ts` | 暴击单结算 ×4 场景、物理/真伤公式、吸血语义、DoT 类型、超时裁定（含平局与比例胜者） |
| `conservation.test.ts` | 卡池守恒、合成消耗、装备返还、自动整理溢出、买入回滚 + 卖出对称性 |
| `economy.test.ts` | 收入构成、利息上限、连败追平连胜、经验表 |
| `undo.test.ts` | 快照往返、多步撤销、异常安全 |
| `determinism.test.ts` | 事件流逐字节一致、seed 分歧性 |
| `grid.test.ts` | 占格完整性、寻路步进、越界 |
| `sweep.test.ts` | 补丁往返 / 非法路径 / tune 优先级 / CRN 一致性 / 参数→结果链路贯通 |
| `traits-big.test.ts` | 墨门兼爱分摊（不递归 / silent 不刷屏 / 自伤不再分摊）、兵家百战击杀成长翻倍 |
| `traits-tier.test.ts` | 档位门控（低于首档=-1、[2,4]/[3,6,9]/[2,5,8] 切入点）、丹鼎 2 人无回蓝、剑宗/龙渊团队效果单次生效 |
| `skills.test.ts` | 11 类技能实现全部有归属棋子 + 每类真实对局冒烟（施法事件 >0、tick 有限） |
| `mech-caps.test.ts` | MECH 机制契约：真伤单跳钳制与处决豁免、反射逐跳衰减、每秒封顶截断、tick 界重置、禁用默认值（0/1/0）为无操作 |
| `golden-determinism.test.ts` | 金种子快照：5 用例同进程双跑逐字段相等 + 与 `tests/__golden__/battle-golden.json` 比对（`UPDATE_GOLDEN=1` 再生） |
| `adventure.test.ts` | 奇遇轮：offer 确定性、奇遇/墨兽轮互斥、恩赐入账守恒（援军占卡池/备战席满折金）、AI 原型偏好确定性、快照记录纯观察者、整局奇遇真实发生 |
| `daily.test.ts` | 每日挑战：todayKey 本地时区与跨日边界、种子同日稳定/跨日分歧/uint32、成绩首记/刷新/不刷新/隐私模式容错 |
| `replay.test.ts` | 回放：真实 Battle 快照 verifyReplay 全过、篡改被捕获、无事件流快照按 winner/ticks 校验、**跨线合流**（真实对局 8 回合全部快照通过校验） |
| `save.test.ts` | 存档 v3：载荷结构、toJSON 往返深度无损、v2 手造载荷无损迁移、写回失败保留旧键、坏档归零 |
| `jiguan-pen.test.ts` | 机关结构通道：穿甲生效/首档门控/0.85 封顶/非成员不受/默认冬眠逐字节一致；第 4 击 giant（maxHp%）与 crush（effArmor%）精确语义与特异性（均默认 0 冬眠，含否决证据链） |

## 4. 模拟验证链（无头统计）

| 层 | 命令 | 通过标准 | 当前值 |
|---|---|---|---|
| 流派 | `npm run sim` | 极差 < 15%，先手 50%±8% | 2026-08-30 极差 **22.4%**（3v1 边已修复至 90.75%；2v4 经穷举证明为结构边，见 DESIGN §十三；先手 48.3%） |
| 克制边 | `npm run sim:ab` | 定向改动移动 ≥2p 才算实质 | 十殿阈值 0.20 修复 3v1（-9.8p）；机关侧全部杠杆穷举证伪（证据链入 traits.ts 注释） |
| 装备 | `npm run sim:items` | 合成正收益全员、极差 < 16%、同件递减、协同有界 | **22 件全正收益**、合成 14/14 正、无越界（法符/拳套/玄甲/不朽衣已调平，见 CHANGELOG 1.2.0） |
| 对局 | `npm run sim:match` | 局长 25~40 分钟、AI 原型极差 < 1.0 | **25.5 分钟**（110 血 + 后期曲线 0.75@16 入带），0.84 |
| 数据 | `npm run audit` | 全部羁绊最高档可达、无重复组合 | ✓ |
| 大羁绊 | `npm run sim:big` | 强于预设但非统治（平均 72%~80% 已在门内） | 墨门 74.5%，兵家 69.3% |

度量方法注记（曾引起误判的三个坑）：

- **内存泄漏判定看趋势不看单点**：40 次场景重启中出现过单轮 +167KB 的读数，
  跑满 40 轮后堆反而低于起始值 —— GC 噪声。结构性指标（显示列表 / 更新列表 /
  纹理数）持平才是判据。
- **WebGL canvas 读像素**必须开 `preserveDrawingBuffer` 或在 rAF 回调里读，
  否则拿回全黑 —— 曾差点误判生产构建白屏。
- **console 采集只抄不吞**：报错/警告抄一份到 ring buffer，原始 console 行为不变，
  "零报错"是对真实控制台说的。

## 5. 性能包络

### 5.1 战斗场景（22 单位同屏，1920×1080）

| 指标 | 值 |
|---|---|
| 帧间隔均值 / P95 / 最大 | 6.6 / 7.5 / 8.0 ms |
| 超 16.7ms 的帧 | 0% |
| 解锁帧率上限后 | 181 FPS |
| 每帧实际工作耗时 | 4.67 ms（预算占用 28%） |

### 5.2 准备阶段（满编棋盘 + 满器匣）

| 指标 | 值 |
|---|---|
| 每帧工作耗时 | 6.44 ms |
| 解锁帧率 | 130.8 FPS |
| 实时 Graphics 指令 | ~9,700 条（棋盘格 / 备战席 / 棋子卡均已烘焙为纹理） |

性能纪律：**形态不随帧变化的图形一律烘焙**（`render/bake.ts`）。WebGL 的
`Graphics` 每帧重放整个命令缓冲，一个圆角矩形 ≈ 45 条命令 —— 棋盘格层
曾是 9,848 条指令独占 8.6ms/帧，烘焙后归零。

### 5.3 生产构建

| 项 | 值 |
|---|---|
| app 包 | ~72KB gzip（phaser 单独分包 ~340KB） |
| 页面加载 / 遮罩移除 / 实际渲染 | ✓（像素采样与开发版一致） |

## 6. 稳定性

| 检查项 | 结果 | 方法 |
|---|---|---|
| 40 次场景重启内存 | ✓ 无泄漏（堆 92.7MB < 起始 105MB，结构指标持平） | `window.__arena` 重启循环 |
| 控制台 | ✓ 零 error / 零 warn | ring buffer 抄送 + 原始控制台 |
| 存档往返 | ✓ 中途回合准确恢复（含 HMR 意外重载路径） | `game/save.ts` |
| 墨兽轮 | ✓ 同兽同阵容、掉落与装备守恒 | `npm run beast` |
| 整局无死锁 | ✓ 300 局全部正常结束，无超上限回合 | `npm run sim:match` |

## 7. 音频

16 个音效 + 3 场景 BGM 全部程序化生成（无音频文件）；三总线（bgm/sfx/ui）
独立调节 + 输出限幅器 + 后台自动挂起。逐项播放与切换检查通过。

## 8. 实验控制台（仅 `npm run dev`）

`Ctrl+~` 唤起/关闭：+50 金 / +1 级 / +20 生命 / random 2★ / 备战席全 2★ / 1 full bag / 快进到底 / 清场。全部经 `GameScene.handleDebug` 走存量对局数据，实机路径不受污染。

## 9. 视觉验收

界面为 Canvas 程序化绘制，视觉验收 = 运行 `npm run dev` 后对以下要点过目：

- 准备场景：面板空态文案、朱印出售区、商店卡剪影配色（七色族）、无文字重叠/截断；
- 战斗场景：敌我边缘光（青瓷/朱砂）、血条、飘字与顶栏无重叠；
- 色相纪律：全项目禁用高饱和荧光色（蓝/绿/紫），规范见 ART_BIBLE §2.6；
- 三星五费全屏演出「天命之印」完整播放。

## 9. 已知限制

见 **[DESIGN.md §十三](./DESIGN.md)** —— 局长 23.8 分钟偏低、极端克制边为机制锁定、
防御装胜率口径结构性偏低、准备阶段实时指令余量等，均附处理路径。

## 10. 复现命令速查

```
npm run typecheck          # 类型
npm test                   # 158 项回归
npm run build              # 生产构建
npm run sim 200            # 流派矩阵（每对 200 局）
npm run sim:items 16       # 装备三层检验
npm run sim:sweep scripts/sweeps/sensitivity.json --n 60
npm run sim:ab -- --set trait.jiguan.armor=16 --n 150
npm run sim:match 200      # 整局模拟
npm run gold               # 预设造价带
npm run beast              # 墨兽轮自检
npm run diag 0 1 4242      # 单场逐单位诊断
npm run probe 0 1 60       # 对阵分布探针
```
